[project]
name = "blender-python-modeling-tools"
version = "0.1.0"
description = "Host-side reference image analysis tools for the blender-python-modeling skill"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24,<3",
  "opencv-python-headless>=4.8,<5",
]

[tool.uv]
package = false
